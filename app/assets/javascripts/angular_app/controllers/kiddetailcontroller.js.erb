(function(){
  var app = angular.module('kid-details', []);

  app.controller('KidDetailController', [ '$http', function($http){
    kiddetails = this;
    kiddetails.kid = {};

    kiddetails.show = function(){
      $http.get('/api/v1/kids/' + kidlist.current_kid.id)
        .success(function(data){
          kiddetails.kid = data;
        });
    };
  }]);
})();
