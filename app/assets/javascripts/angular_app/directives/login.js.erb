(function() {
  var app = angular.module('login-directives', []);

  app.directive("loginForm", function() {
    return {
      restrict: 'E',
      templateUrl: '<%= asset_path("angular_app/templates/login-form.html") %>'
    };
  });
})();
